import java.util.*;
class Solution {
    public List<Integer> replaceWithRank(List<Integer> arr) {
        // Your code goes here
        intn=arr.size();
        int[] arr1=new int[n];
        for(int i=0;i<n;i++)
        {   arr1[i]=list.get(i);
        }
        Arrays.sort(arr1);
        HashMap<Integer,Integer> map=new HashMap<>();
        for(inti=0;)
    }
}
